{
    "buildCommand": "cd loopin-web && npm run build",
    "outputDirectory": "loopin-web/dist",
    "installCommand": "cd loopin-web && npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}