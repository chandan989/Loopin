{
    "builds": [
        {
            "src": "loopin-web/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "loopin-web/dist"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}